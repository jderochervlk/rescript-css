{
  "name": "example-react",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run rescript:build && vite build",
    "rescript:build": "pnpm run rescript:package && rescript clean && rescript build",
    "rescript:package": "cd ../../packages/rescript-css && ../../node_modules/.bin/rescript clean && ../../node_modules/.bin/rescript build",
    "dev": "vite"
  },
  "dependencies": {
    "@jvlk/rescript-css": "workspace:*",
    "@rescript/react": "^0.15.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
