{
  "name": "example-rescript-11",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run rescript:build && vite build",
    "rescript:build": "pnpm run rescript:package && rescript clean && rescript build",
    "rescript:package": "cd ../../packages/rescript-css && ../../examples/rescript-11/node_modules/.bin/rescript build",
    "dev": "vite"
  },
  "dependencies": {
    "@jvlk/rescript-css": "workspace:*"
  },
  "devDependencies": {
    "@rescript/runtime": "^12.0.0",
    "rescript": "11.1.4",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
