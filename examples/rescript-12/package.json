{
  "name": "example-rescript-12",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm run rescript:build && vite build",
    "rescript:build": "pnpm run rescript:package && rescript clean && rescript build",
    "rescript:package": "cd ../../packages/rescript-css && ../../examples/rescript-12/node_modules/.bin/rescript build",
    "dev": "vite"
  },
  "dependencies": {
    "@jvlk/rescript-css": "workspace:*"
  },
  "devDependencies": {
    "rescript": "12.3.1",
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
